import hotdog from '../../img/783.gif'

const Spinner = () => {
    return(
        <div style={{display: 'flex' ,width: '100%', textAlign: "center"}}>
            <img src={hotdog} alt=""style={{margin: '0 auto'}}/>
        </div>
    )
}

export default Spinner